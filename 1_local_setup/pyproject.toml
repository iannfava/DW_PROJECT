[project]
name = "1-local-setup"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "dbt-core>=1.10.15",       #DBT core
    "dbt-postgres>=1.9.1",     #Postgres adaptor for DBT
    "duckdb>=1.4.3",           #local analytical databse 
    "faker>=38.2.0",           #fake data generator 
    "numpy>=2.3.5",
    "pandas>2.3.3",
]